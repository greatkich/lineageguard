version: 2

models:
  - name: customer_features
    description: Production fraud features keyed by customer identifier.
    columns:
      - name: customer_id
        data_tests: [not_null, unique]
      - name: order_count
        data_tests: [not_null]
      - name: max_order_total
        data_tests: [not_null]
