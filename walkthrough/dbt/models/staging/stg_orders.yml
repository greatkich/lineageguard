version: 2

models:
  - name: stg_orders
    description: Typed staging projection of commerce orders.
    columns:
      - name: order_id
        data_tests: [not_null, unique]
      - name: customer_id
        data_tests: [not_null]
